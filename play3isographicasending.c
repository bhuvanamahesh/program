#include<stdio.h>
void main()
{
int i,j,a,n,num[i];
scanf("%d",&n);
scanf("%d",&num[i]);
for(i=0;i<n;++i)
{
	for(j=0;j<n;++j)
	{
		if(num[i]>num[j])
		{
			a=num[i];
			num[i]=num[j];
			num[j]=a;
		}
		for(i=0;i<n;++i)
		{
			scanf("%d",&num);
		}
	}

return 0;
}
}
