#include <stdio.h>
int main()
{
   char a[100];
   int i,n,f=0;
   gets(s);
   n=strlen(s);
   for(i=0;i<n;i++)
   {
       if(a[i]==' ')
       {
           
       a[i]=a[i+1];
       
       printf("%c",a[i]);
       
       }
       else
       printf("%c",a[i]);
   }
    return 0;
}
